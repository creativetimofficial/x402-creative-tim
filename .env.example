# ===================================
# X402 Creative Tim API Configuration
# ===================================

# Network Configuration
# Options: 'testnet' (Base Sepolia) or 'mainnet' (Base)
# Defaults to mainnet - set to 'testnet' for safe testing with free tokens
# You can also use ?testnet=true in the URL to switch to testnet mode
NETWORK=mainnet

# ===================================
# Wallet Addresses (Required)
# ===================================
# These are the addresses where you'll receive payments
# Use the same Ethereum address for both networks

# Mainnet wallet (Base) - for production with real payments
MAINNET_WALLET_ADDRESS=0xYourWalletAddress

# Testnet wallet (Base Sepolia) - for testing with free tokens
TESTNET_WALLET_ADDRESS=0xYourWalletAddress

# ===================================
# Public Wallet Addresses (Required for client-side)
# ===================================
# These MUST match the addresses above
# They're used by the frontend for payment preparation

NEXT_PUBLIC_MAINNET_WALLET_ADDRESS=0xYourWalletAddress
NEXT_PUBLIC_TESTNET_WALLET_ADDRESS=0xYourWalletAddress

# ===================================
# Coinbase Developer Platform API Keys
# ===================================
# Required ONLY for mainnet (production) - these enable the CDP-hosted facilitator
# Get these from: https://portal.cdp.coinbase.com/
# The CDP facilitator handles payment verification and settlement to your Coinbase Business account
# Leave commented out when using testnet (community facilitator doesn't need keys)

CDP_API_KEY_ID=your-cdp-api-key-id
CDP_API_KEY_SECRET=your-cdp-api-key-secret

# ===================================
# OnchainKit API Key (Optional but Recommended)
# ===================================
# Provides enhanced features and better UX
# Get one at: https://portal.cdp.coinbase.com/products/onchainkit
NEXT_PUBLIC_ONCHAINKIT_API_KEY=your-onchainkit-api-key

# ===================================
# App URL (Required)
# ===================================
# The base URL where your app is hosted
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===================================
# UI/UX Book Download URLs
# ===================================
# URLs for the paid and preview versions of the UI/UX book
# These are kept in environment variables to prevent public exposure

# Full book download URL (paid content - server-side only)
BOOK_URL_PAID=https://www.dropbox.com/your-paid-content-url

# Preview/sample chapters URL (public - safe to expose to client)
NEXT_PUBLIC_BOOK_URL_PREVIEW=https://www.dropbox.com/your-preview-url
